# Lower bounds are set to versions that carry known security fixes; keep them current.
fastapi>=0.115.6         # pulls a starlette without the multipart DoS (CVE-2024-47874)
uvicorn>=0.30
sqlalchemy>=2.0
pydantic-settings>=2.4
jinja2>=3.1.5            # CVE-2024-56201 / CVE-2024-56326
python-multipart>=0.0.18  # CVE-2024-53981 (multipart DoS)
itsdangerous>=2.2
httpx>=0.27
python-ulid>=2.7
tzdata>=2024.1  # zoneinfo fallback where no OS tz database exists (e.g. Windows dev)
